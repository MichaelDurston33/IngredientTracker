import React from 'react';

import classes from './RememberBlocksBack.css';

const RememberBlocks = (props) =>(
  <div className={classes.RememberBlockBack}>{props.children}</div>
  )


export default RememberBlocks
